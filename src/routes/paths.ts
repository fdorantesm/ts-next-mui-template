/**
 * Application route paths.
 * For Next.js App Router, these should match the file structure in /app directory.
 */
export const paths = {
  home: "/",
  // Add more paths as needed, e.g.:
  // about: '/about',
  // blog: '/blog',
};
